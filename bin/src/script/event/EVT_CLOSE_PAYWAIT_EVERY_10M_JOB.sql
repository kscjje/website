CREATE DEFINER=`root`@`%` EVENT `EVT_CLOSE_PAYWAIT_EVERY_10M_JOB`
	ON SCHEDULE
		EVERY 10 MINUTE STARTS '2021-11-21 16:20:05'
	ON COMPLETION PRESERVE
	ENABLE
	COMMENT '10분마다 돌아, EDC_RSVN_INFO.EDC_PAYWAIT_ENDDATETIME 를 체크하여 "기한만료자동취소'
	DO CALL `SP_CLOSE_PAYWAIT`()
	
/*
-- event_scheduler 활성화 명령어
SELECT * FROM INFORMATION_SCHEMA.EVENTS;
SHOW VARIABLES LIKE 'event%';
SET GLOBAL event_scheduler = ON;
SET @@global.event_scheduler = ON;
*/